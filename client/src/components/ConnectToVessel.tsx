import React, { FC } from 'react';

export const VesselUser: FC = () => (
  <p>
    {`A popup should be displayed asking to share your attestations. If you've cancelled out of the popup, remove it from your Connected Sites to enable it again.`}
  </p>
);
